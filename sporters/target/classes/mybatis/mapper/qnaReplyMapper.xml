<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gdu.sporters.userSupport.mapper.QnaReplyMapper">

	<select id="selectReplyList" parameterType="Map" resultType="QnaReplyDTO">
		SELECT QNA_REPLY_NO, QNA_NO, QNA_REPLY_CONTENT, QNA_REPLY_CREATE_DATE
		FROM QNA_REPLY
	</select>
	
	<insert id="insertReply" parameterType="QnaReplyDTO">
		INSERT INTO QNA_REPLY
			(QNA_NO, QNA_REPLY_CONTENT, QNA_REPLY_CREATE_DATE)
		VALUES
			(#{qnaNo}, #{qnaReplyContent}, NOW())
	</insert>
	
	<delete id="deleteReply" parameterType="int">
		DELETE QNA_REPLY
		 WHERE QNA_REPLY_NO = #{qnaReplyNo}
	</delete>
	
</mapper>